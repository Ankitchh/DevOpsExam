apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
    env: blue
  ports:
    - port: 80
      targetPort: 3000

~                                                                               
~                                                                               
~             